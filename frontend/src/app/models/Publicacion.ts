import { Title } from '@angular/platform-browser';

export interface Publicacion {
    idPublicacion?: number,
    fecha?: Date,
    idUsuario?: number,
    idJuego?: number,
    comentario?: string
};