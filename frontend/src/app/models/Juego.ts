import { Title } from '@angular/platform-browser';

export interface Juego {
    idJuego?: number,
    nombre?: string,
    descripcion?: string,
    cartucho?: string,
    fecha?: Date,
    idConsola?: number
};